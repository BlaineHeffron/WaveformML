void cffi_window_edges(
    const int n, //square window size
    long long* edge_idx,
    const int num_elem, //square window size
    const int* x,
    const int* y,
    const int* b,
    bool self_loop,
    long long* edges1,
    long long* edges2
);